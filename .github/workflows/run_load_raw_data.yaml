name: Run job for loading raw data

on:
  schedule:
    - cron: "* * * * *" # change this.

jobs:
  raw_data:
    runs-on: ubuntu-latest

    permissions:
      contents: write
    
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 